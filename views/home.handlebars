<h1>Itens cadastrados 😷</h1>
<main>
<section class="one">
    <h1>Usuários Cadastrados 😷</h1>
<div class="container">
    {{#each livro}}
    <div class="item">
        <h2>{{this.titulo}}</h2>
        <p>{{this.autor}}</p>

        <p>Preço: {{this.preco}}</p>
        <div class="actions">
           <a href="/livro/{{this.id}}" class="details-btn">Ver detalhes</a>
           <a href="/livro/edit/{{this.id}}" class="edit-btn">Editar</a>
             <form action="/livro/delete/{{this.id}}" method="post">
                <input type="submit" value="Excluir" class="delete-btn">
            </form>
        </div>
    </div>
    {{/each}}
</div>
</section>

<section class="two">
    <h1>Livros Cadastrados 😷</h1>
<div class="container">
    {{#each livro}}
    <div class="item">
        <h2>{{this.titulo}}</h2>
        <p>{{this.autor}}</p>

        <p>Preço: {{this.preco}}</p>
        <div class="actions">
           <a href="/livro/{{this.id}}" class="details-btn">Ver detalhes</a>
           <a href="/livro/edit/{{this.id}}" class="edit-btn">Editar</a>
             <form action="/livro/delete/{{this.id}}" method="post">
                <input type="submit" value="Excluir" class="delete-btn">
            </form>
        </div>
    </div>
    {{/each}}
</div>
</section>
</main>